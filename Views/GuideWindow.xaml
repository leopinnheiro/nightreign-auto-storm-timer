<Window x:Class="nightreign_auto_storm_timer.Views.GuideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:nightreign_auto_storm_timer.Views"
        mc:Ignorable="d"
        Width="576"
        Height="162"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        >
    <Grid>
        <Rectangle Stroke="#5b21b6" StrokeThickness="4" RadiusX="0" RadiusY="0" StrokeDashArray="6, 3"/>
        <Grid>
            <!-- Seu conteúdo, inclusive o Border interno -->
            <Border
            CornerRadius="4" Background="#fafafa"
            Width="24" Height="24"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Margin="4"
            Cursor="SizeAll"/>
        </Grid>
    </Grid>
</Window>
